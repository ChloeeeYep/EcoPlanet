<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Subscription Confirmation</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .wrapper {
            text-align: center;
            background-color: #fff;
            padding: 40px;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            position: absolute; /* Position it absolutely within the body */
            top: 50%; /* 50% from the top of the viewport */
            left: 50%; /* 50% from the left of the viewport */
            transform: translate(-50%, -50%); /* Offset the element by half its width and height */
            width: calc(100% - 80px); /* Responsive width with padding */
            max-width: 875px; /* Maximum width of the wrapper */
            box-sizing: border-box; /* Include padding and border in the element's total width and height */
        }

        .icon {
            width: 50px;
            height: 50px;
            margin-bottom: 20px;
            background-image: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 16 16" fill="%2328a745"><path fill-rule="evenodd" d="M13.854 3.646a.5.5 0 0 1 0 .708l-7 7a.5.5 0 0 1-.708 0l-3.5-3.5a.5.5 0 1 1 .708-.708L6.5 10.293l6.646-6.647a.5.5 0 0 1 .708 0z"/></svg>');
            background-size: cover;
            display: inline-block;
        }

        h1, h3 {
            color: #333;
        }

        p {
            color: cornflowerblue;
        }

        .btn-primary {
            background-color: #007bff;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
            display: inline-block;
            text-decoration: none;
        }

        .btn-primary:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>

    <div class="wrapper">
        <div class="icon"></div>
        <h1>Thank you for subscribing to our newsletter!</h1>
        <h3>To confirm, you need to open the email and click the confirm button!</h3>
        <p>Your request ID for this subscription: <span>@ViewBag.requestID</span></p>
        <form>
            <button type="submit" class="btn-primary" asp-action="Index" asp-controller="SNS">
                Back to the subscription page!
            </button>
        </form>
    </div>

</body>
</html>
